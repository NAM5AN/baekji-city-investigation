import assert from "node:assert/strict";
import fs from "node:fs";

const html = fs.readFileSync(new URL("../admin-dashboard.html", import.meta.url), "utf8");
const source = fs.readFileSync(new URL("../admin-zone-map.js", import.meta.url), "utf8");
const css = fs.readFileSync(new URL("../admin-zone-map.css", import.meta.url), "utf8");
const liveRender = fs.readFileSync(new URL("../admin-live-render.js", import.meta.url), "utf8");
const scrollSource = fs.readFileSync(new URL("../admin-scroll-stability.js", import.meta.url), "utf8");
const logChronology = fs.readFileSync(new URL("../admin-log-bottom-chronology.js", import.meta.url), "utf8");
const mobileCss = fs.readFileSync(new URL("../admin-mobile-shell-polish.css", import.meta.url), "utf8");
const mobileTopbar = fs.readFileSync(new URL("../admin-mobile-topbar.js", import.meta.url), "utf8");
const playerMap = fs.readFileSync(new URL("../assets/maps/haeoreum-day1-map.svg", import.meta.url), "utf8");

assert.match(html, /admin-zone-map\.css\?v=0\.6\.1/, "admin dashboard must load compact zone-map styles with a fresh cache key");
assert.match(html, /admin-zone-topology\.js\?v=0\.1\.0&stage4b=1/, "admin dashboard must load shared zone topology before its consumers");
assert.match(html, /admin-zone-map\.js\?v=0\.6\.1&stage4b=1/, "admin dashboard must load zone-map runtime with a fresh cache key");
assert.match(html, /admin-live-render\.js\?v=0\.6\.4&stage4b=1/, "admin dashboard must load no-flicker live renderer");
const topologyScriptIndex = html.indexOf("admin-zone-topology.js?v=0.1.0&stage4b=1");
const mapScriptIndex = html.indexOf("admin-zone-map.js?v=0.6.1&stage4b=1");
const liveScriptIndex = html.indexOf("admin-live-render.js?v=0.6.4&stage4b=1");
assert.ok(topologyScriptIndex >= 0, "topology cache key must be present before ordering is checked");
assert.ok(mapScriptIndex >= 0, "map cache key must be present before ordering is checked");
assert.ok(liveScriptIndex >= 0, "live cache key must be present before ordering is checked");
assert.ok(topologyScriptIndex < mapScriptIndex, "topology must load before zone-map consumer");
assert.ok(mapScriptIndex < liveScriptIndex, "live renderer must load after the zone-map runtime");
assert.match(html, /admin-scroll-stability\.js\?v=0\.6\.1/, "admin dashboard must load scroll-stability runtime after the zone map");
assert.ok(html.indexOf("admin-scroll-stability.js?v=0.6.1") > html.indexOf("admin-live-render.js?v=0.6.4&stage4b=1"), "scroll stability must observe the stable live-render lifecycle");
assert.match(html, /admin-log-bottom-chronology\.js\?v=0\.6\.5/, "admin dashboard must load chronological log runtime with a fresh cache key");
assert.ok(html.indexOf("admin-log-bottom-chronology.js?v=0.6.5") > html.indexOf("admin-scroll-stability.js?v=0.6.1"), "chronological log runtime must load after scroll restoration");
assert.ok(html.indexOf("admin-log-bottom-chronology.js?v=0.6.5") > html.indexOf("admin-log-recipient-grouping.js?v=0.6.4"), "chronological log runtime must run after recipient grouping is available");
assert.doesNotThrow(() => new Function(liveRender), "admin live renderer must parse as JavaScript");
assert.match(source, /const VERSION = "0\.6\.1"/, "zone map runtime version must match its Stage 4-B cache key");
assert.match(liveRender, /const VERSION = "0\.6\.4"/, "live renderer version must match its Stage 4-B cache key");
assert.match(liveRender, /Object\.defineProperty\(panel, "innerHTML"/, "polling panel writes must be intercepted instead of replacing the whole DOM");
assert.match(liveRender, /syncChildren\(panel, fragment\)/, "ordinary admin tabs must update their existing DOM incrementally");
assert.match(liveRender, /baekji-admin-zone-live-source/, "enhanced zone maps must receive live state without being torn down");
assert.match(liveRender, /topology\.recordFromCard/, "zone occupancy source cards must use the shared topology parser");
assert.match(liveRender, /topology\.routeRecordMap/, "zone routes must use the shared topology route map");
assert.match(liveRender, /shouldPreserveControl/, "log filters and focused controls must survive live synchronization");
assert.doesNotThrow(() => new Function(scrollSource), "admin scroll stability runtime must parse as JavaScript");
assert.match(scrollSource, /document\.addEventListener\("scroll"/, "scroll positions must be captured while the operator scrolls");
assert.match(scrollSource, /new MutationObserver\(scheduleRestore\)/, "snapshot rerenders must trigger scroll restoration");
assert.match(scrollSource, /admin-panel-scroll/, "main tab scroll position must be preserved");
assert.match(scrollSource, /data-admin-zone-map-viewport/, "zone-map horizontal and vertical scroll must be preserved");
assert.match(scrollSource, /data-admin-log-list/, "log list scroll must be preserved");
assert.match(scrollSource, /admin-mini-list/, "overview list scroll must be preserved");
assert.doesNotThrow(() => new Function(logChronology), "admin chronological log runtime must parse as JavaScript");
assert.match(logChronology, /recipientGroupingVersion/, "chronology must wait until grouped SYSTEM rows are finalized");
assert.match(logChronology, /rows\.reverse\(\)\.forEach/, "admin logs must be reordered from oldest to newest");
assert.match(logChronology, /scrollTop = Math\.max\(0, list\.scrollHeight - list\.clientHeight\)/, "log view must open at the newest row on the bottom");
assert.match(logChronology, /pinBottom = isNearBottom\(target\)/, "manual upward scrolling must disable forced bottom pinning");
assert.match(source, /assets\/maps\/haeoreum-day1-map\.svg\?v=0\.6\.0/, "admin map must reuse the player investigation map topology asset");
assert.match(source, /data-admin-zone-map/, "zone tab must be replaced by an interactive topology map shell");
assert.match(source, /data-admin-detail/, "map nodes must keep the existing admin detail popup contract");
assert.match(source, /is-occupied-route/, "occupied movement routes must be highlighted on the topology map");
assert.match(source, /현재 세부 현장 · 이동 구간/, "detail and route occupancy must remain visible under the topology map");
assert.match(source, /구역 목록으로 보기/, "a compact list fallback must remain available");
assert.match(css, /admin-zone-map-viewport svg \[data-node\]\.admin-zone-node\.is-occupied/, "occupied nodes must have a distinct visual state");
assert.match(css, /path\.route\.is-occupied-route/, "occupied routes must have a distinct visual state");
assert.match(css, /width:min\(100%,980px\)/, "desktop admin map must be capped so the full topology is easier to scan at once");
assert.match(css, /@media\(max-width:900px\).*min-width:720px/s, "small screens must retain the pannable minimum map width");
assert.match(playerMap, /data-node="E_ENTRY"/, "player map must expose the canonical entry node");
assert.match(playerMap, /data-from="E_G_PLAZA" data-to="E_G_EAST"/, "player map must expose route topology through from/to attributes");

assert.match(html, /admin-mobile-shell-polish\.css\?v=0\.6\.2/, "admin dashboard must load mobile shell polish styles");
assert.match(html, /admin-mobile-topbar\.js\?v=0\.6\.2/, "admin dashboard must load mobile topbar runtime");
assert.ok(html.indexOf("admin-mobile-topbar.js?v=0.6.2") > html.indexOf("admin-world-reset.js?v=0.5.1"), "mobile topbar runtime must load after admin action buttons are installed");
assert.doesNotThrow(() => new Function(mobileTopbar), "admin mobile topbar runtime must parse as JavaScript");
assert.match(mobileCss, /grid-template-areas:"brand actions" "meta meta"/, "mobile topbar must use a compact two-row layout");
assert.match(mobileCss, /data-admin-world-reset-open/, "topbar admin action buttons must be hidden behind the compact operations menu on mobile");
assert.match(mobileCss, /padding:12px 12px calc\(56px \+ env\(safe-area-inset-bottom\)\)/, "mobile panel must reserve bottom breathing room");
assert.match(mobileCss, /scroll-padding-bottom:calc\(56px \+ env\(safe-area-inset-bottom\)\)/, "nested mobile scrollers must retain visible bottom space");
assert.match(mobileTopbar, /data-admin-mobile-proxy="mvp5"/, "mobile operations menu must retain session operations access");
assert.match(mobileTopbar, /data-admin-mobile-proxy="audit"/, "mobile operations menu must retain audit log access");
assert.match(mobileTopbar, /data-admin-mobile-proxy="reset"/, "mobile operations menu must retain reset access");

console.log("PASS: admin live sync keeps compact map, scroll stability, and oldest-to-newest bottom-pinned logs wired");
