# 백지도시 리팩터링 테스트 기준선

이 문서는 기능을 추가하기 위한 명세가 아니라, 코드 정리/개선 과정에서 **기존 동작이 깨지지 않았는지 확인하기 위한 기준선**이다.

## 자동 검사

- `npm run check:syntax` — 저장소의 JS/MJS/CJS 전체 문법 검사
- `npm run test:core` — 공통 데이터·기본 조사 규칙
- `npm run test:player` — 로그인 이후 조사조·위험·아이템 등 플레이어 기능
- `npm run test:sync` — 월드 동기화·관찰·소음 로그
- `npm run test:admin` — 관리자 관제·통신·제어
- `npm run test:ui` — 모바일·모션·시각 UI
- `npm run test:server` — 서버/API/Vercel/GitHub Pages 계약
- `npm run check` — 위 그룹 전체 실행

GitHub Actions의 멀티탭/인증/Supabase live probe 계열 진단은 별도 단계로 유지한다.

## 최소 수동 스모크 테스트

### 플레이어

1. 테스터 캐릭터로 로그인한다.
2. 조사조를 생성하고 다른 캐릭터를 초대한다.
3. 상대 캐릭터가 초대를 수락하고 구성 확인·준비를 완료한다.
4. 조장이 조사 세션을 시작한다.
5. 조사 화면에서 일반 대화 1회와 `/` 행동 1회를 입력한다.
6. 연결된 장소로 1회 이동한다.
7. 새로고침 후 조사조·세션·위치가 유지되는지 확인한다.

### 관리자

1. 관리자 로그인 후 전체 현황이 열린다.
2. `구역`, `조사조`, `캐릭터`, `로그` 탭이 모두 열린다.
3. 구역 상세 팝업이 열리고 조사조·인원 탭의 프로필/이름이 정상 표시된다.
4. 로그 탭은 과거가 위, 최신이 아래이며 처음 열 때 최하단에 위치한다.
5. 동일 SYSTEM 로그는 한 줄로 묶이고, 수신자별 관찰 로그는 따로 유지된다.

## 대형 구조 변경 후 추가 확인

WorldStore, Storage hook, cloud sync, server authority처럼 상태 처리 구조를 바꾸는 단계에서는 PC 2개 탭 + 모바일 1대 수준의 동시 접속 테스트를 추가한다.

- 한 기기의 변경이 다른 기기에서 되돌아가지 않는다.
- 탈퇴/강퇴된 조사조원이 오래된 상태 때문에 되살아나지 않는다.
- 관찰문·SYSTEM 로그가 중복되거나 잘못된 행동 밑에 붙지 않는다.
- 새로고침 후 최종 상태가 fallback/이전 값으로 복귀하지 않는다.

## 리팩터링 원칙

1. 한 PR에는 하나의 구조 변화만 넣는다.
2. 새 구조를 검증하기 전에 기존 안전장치를 삭제하지 않는다.
3. 사용자에게 보이는 동작을 의도적으로 바꾸는 변경은 정리 PR과 분리한다.
4. 자동 검사 통과 후 필요한 수동 스모크 테스트를 완료하고 다음 단계로 넘어간다.
