# 변경 기록

## v0.3.18

- 일러스트/SYSTEM 높이 조절용 가로 드래그 분할선 추가
- 왼쪽 조사 영역/오른쪽 채팅 영역 너비 조절용 세로 드래그 분할선 추가
- 조사 화면 분할 비율 저장·키보드 조절·더블클릭 초기화 지원
- 채팅 탭의 중복 조사조 요약 헤더 제거
- 현재 장소 재이동 입력을 `이미 …에 있다` SYSTEM 문장으로 분리
- 비인접 목적지에 최단 연결 경로 첫 방향과 전체 순서 안내
- 목적지가 명시된 경우 무관한 행동 선택지 공개 차단

## v0.3.17

- 활성 세션의 SYSTEM·조사 채팅 로그 잘라내기 제거
- 재접속·새로고침·행동 결과·AI 결과문 갱신 시 로그 최하단 안정화
- AI 행동 스키마에 행동 주체·감각 방식·실제 대상·실행 가능성·위험 관련성 추가
- 듣기·자기 상태 확인·기다리기·위험 대응을 독립 의도로 분리
- 위험과 무관한 행동의 위험 진행·이동·오염 변화를 차단
- 결정 규칙이 확정한 결과만 바탕으로 하는 AI 몰입형 결과문 생성 API 추가
- 판정투 표현을 제거한 행동별 로컬 결과문 보강

## v0.3.16

- 1번 본문 폰트를 DungGeunMo에서 Y콤퓨타체 원본 OTF로 교체
- 로그인부터 조사 결과까지 전 화면 `--font-body` 역할에 새 폰트 적용
- SYSTEM 로그·조사 채팅·입력창의 본문 폰트 연결 유지
- OTF 정적 파일 MIME 형식과 캐시 버전 갱신
- Y콤퓨타체 원본 서체 가이드·라이선스 PDF 포함
- 원본 OTF 형식·전 화면 preload·본문 역할 자동 검증 갱신

## v0.3.15

- `/행동` 원문을 캐릭터 아이콘·이름이 포함된 SYSTEM 로그 항목으로 저장
- 행동 문장에 명시된 사용 물건 이름과 실제 소지품 ID를 로컬/AI 판정에 추가
- 소지품 수량·상태·용도 태그를 이용한 물건 사용 가능성 선행 검사 추가
- 미소지·사용 불가·용도 부적합 시 이동과 위험 해결 전에 자연스러운 실패 결과 출력
- 사용 가능한 물건의 준비 행동을 이동·위험·조사 서술 앞에 연결
- 행동 기록·미소지 손수건·보유 휴지·부적합 볼펜 회귀 검사 추가

## v0.3.14

- 전역 점무늬 오버레이와 텍스트 설명 영역의 줄무늬 배경 제거
- 지도 구역 라벨의 격자·사선 패턴 및 장거리 점선 경로 제거
- 지상·B1·B2 층별 허브와 분기 동선 중심으로 건물 안내도 재구성
- 계단마다 연결 층·목적지를 직접 표기하고 이동 경로를 실선 양방향 화살표로 변경
- 텍스트 단색 배경과 무패턴 지도 자동 검증 추가

## v0.3.13

- `run.bat`의 중첩 PowerShell 환경 변수 로딩 구문 제거
- Windows 사용자 환경 변수 레지스트리를 서버가 직접 읽도록 변경
- Node.js 미설치·압축 미해제·서버 종료 시 배치 창 유지 및 진단 출력 추가
- Windows 환경 변수 자동 로딩 단위 테스트 추가

## v0.3.12

- 환경 변수 기반의 로컬 Node 서버와 OpenAI Responses API 행동 의도 판정 추가
- 구어체·축약형·오타·다양한 어미를 수용하는 하이브리드 로컬/AI 판정 추가
- AI가 의도·후보 대상만 선택하고 게임 결과는 기존 규칙이 결정하도록 권한 분리
- 위험 중 지도·관찰·길 묻기와 실제 위험 대응을 분리
- API 미설정·오류 시 로컬 판정 자동 전환 및 조사 입력창 AI 상태 표시
- `run.bat`에서 Windows 사용자 환경 변수의 API 키를 재입력 없이 로드

## v0.3.11

- 위험 상황 입력에서 지도·길찾기·관찰·위험 대응 의도를 순서대로 분리
- `/지도 봄`을 포함한 지도 보기 명사형·축약형 표현 지원
- 층별 방·복도·출입문·계단실 중심의 건물 평면 안내도로 지도 재제작
- 지도 SVG의 현재 노드에 붉은 위치 점 동적 표시
- 동일 변주 전역 아이템 선점 레지스트리와 기존 상태 자동 보정 추가
- 동일 변주에서 획득된 아이템을 다른 캐릭터의 행동 선택지에서 제거
- 간발의 중복 획득 요청에 `이미 누군가 가져가버렸다···` 실패 문장 출력
- 아이템 없는 오브젝트 조사 및 팝업에 `아무것도 없다.` 표시
- 멀티탭 동시 획득 요청을 순서대로 처리하는 Web Locks 적용
- 기존 저장 키와 조사·채팅·이동·위험·행동 선택 계약 유지

## v0.3.10

- SYSTEM 출력문과 시간 표시를 1번 본문 폰트로 고정
- SYSTEM 출력문에 적용되던 3~6번 필기체 순환 제거
- 7번 포인트 폰트를 짧은 배지·상태 수치·창 라벨로 제한
- 실제 조사 노드 11개와 왕복 이동 경로 22개 기반 흑백 SVG 구역 지도 추가
- 조사 화면 상단 `구역 지도` 버튼과 채팅 지도 요청 표현 연결
- 지도 팝업에 현재 위치·이동 중 경로·현재 위치의 직접 연결 목적지 표시
- 지도 노드·경로와 원본 데이터의 일치 여부를 검사하는 회귀 테스트 추가
- 기존 저장 키와 조사·채팅·이동·위험·행동 선택 계약 유지

## v0.3.9

- 조사조 구성·브리핑·결과 페이지에서 레트로 모드가 제거되던 문제 수정
- 조사조 단계·참가자·초대·목적지·브리핑 규칙·결과 카드에 게임보이 UI 적용
- 로그인·브리핑·조사 화면 폰트 역할 선택자 보강
- CSS·스크립트·7종 폰트 URL에 캐시 갱신 버전 추가
- 로그인 접속 단말 버전 표시 갱신
- 기존 저장 키와 조사 기능 계약 유지

## v0.3.8

- 제공된 1번 폰트를 본문·입력·일반 UI에 적용
- 제공된 2번 폰트를 페이지·장소·팝업 타이틀에 적용
- 제공된 3~6번 폰트를 SYSTEM 기록·조사 결과·메모에 순환 적용
- 제공된 7번 폰트를 배지·버튼·수치·경고·강조 요소에 적용
- 외부 CDN 없이 로컬 폰트 자산과 라이선스 파일 동봉
- 손상된 3번 ZIP의 `DOSPilgi-16.bdf`를 복구해 웹용 TTF로 변환
- 기존 저장 키와 조사 기능 계약 유지

## v0.3.7

- 로그인 후 홈 화면을 흑백 게임보이 스타일로 통일
- `본다`·`보다`·`봐`·`훑어본다`를 오브젝트 조사 표현으로 인식
- 등록되지 않은 대상 조사는 실패 대신 평범함·특이점 없음·추가 조사 요소 없음으로 묘사
- 실패 문장에 캐릭터의 실제 행동과 실패 원인을 함께 출력
- 일반 행동 실패와 위험 대응 실패에 유형별 문장 변주 추가
- 기존 저장 키와 조사·채팅·이동·위험·행동 선택 계약 유지

## v0.3.6

- 로그인 화면을 조사 화면과 동일한 흑백 게임보이 스타일로 변경
- 새로고침·조사 복귀·페이지 복원 시 SYSTEM 로그와 조사 채팅을 최하단으로 이동
- 장소·세부 구역·이동 경로·오브젝트별 이미지 매핑 레지스트리 추가
- 매핑 누락·이미지 로드 실패 시 흑백 플레이스홀더 표시
- 오브젝트 조사 직후 이미지 영역이 포함된 상세 팝업 표시
- 조사 완료 오브젝트 상세 다시 보기 지원
- 실제 오브젝트 이미지는 포함하지 않음
- 기존 저장 키와 조사·채팅·이동·위험·행동 선택 기능 유지

## v0.3.5

- 활성 위험 장면을 포함한 동일 이동 경로 scope 동기화
- 경로 합류 시 양쪽 조사조에 마주침 시스템 로그 추가
- 이동 경로 내 조사조 간 일반 채팅 공유 수정
- 미설명 위험 느낌표 배지 제거
- 행동 선택지를 채팅 패널에서 일러스트 우측 하단 오버레이로 이동
- 선택창 닫기·다시 열기 버튼과 짧은 슬라이드 애니메이션 추가
- 세부 조사 지점 scope 반영


## v0.3.4

- 캐릭터 선택형 진입을 아이디/비밀번호 로그인 폼으로 교체
- 테스트 자격 증명: 캐릭터A/B/C, 비밀번호 1234
- 장소별 채팅을 누적 타임라인으로 변경하고 위치 구분선 추가
- 일반 대화 중 길찾기 표현만 힌트 요청으로 좁게 처리
- 관찰·대상 조사·이동·대기·위험 대응 의도를 분리
- 관찰이 이동 완료로 오판되던 문제 수정
- 목적지 없는 이동은 갈림길 묘사와 선택지 공개로 처리
- 위험 판정·불명확 행동·오염 결과 문장을 장면 중심 문체로 개편

# v0.3.3

- 채팅 전송 후 입력창에 내용이 남는 문제 수정
- `/행동` 원문이 다른 캐릭터의 조사 채팅에 표시되는 문제 수정
- 일반 채팅을 위치 범위별 `scopeKey`로 저장·필터링
- 다른 장소에서 오간 채팅이 현재 장소에 표시되는 문제 수정
- 조사·획득 행동 결과를 SYSTEM 창으로 이동
- 관련 회귀 테스트 추가

# v0.3.2

- 일반 채팅과 시스템 행동을 분리했습니다.
- 문장 앞의 `/`를 시스템 판정 트리거로 사용합니다.
- 일반 채팅은 위험 중에도 판정 없이 전달됩니다.
- 선택지 클릭 시 `/행동`이 입력됩니다.

# v0.3.1

- 동일 장소 도착·합류 알림 판정 수정
- 이동 위험 중인 세션을 출발지 현장 인원에서 제외
- 이동 시작 알림과 도착 알림의 시제·장소 의미 정리
- 조사 채팅 한글 IME 입력 끊김 수정
- 시스템·채팅 스크롤 위치 보존
- localStorage/BroadcastChannel 중복 렌더 제거
- 주기적 heartbeat 전체 렌더 제거

## v0.3.0

- 조사 화면을 게임보이풍 흑백 TRPG 레이아웃으로 전면 변경
- 좌측 대형 조사 풍경, 좌측 하단 시스템 메시지창, 우측 캐릭터 채팅 구성
- 기존 이동·주변·교류·소지품·상태·기록 우측 메뉴를 제거
- 우측 패널을 `조사 채팅 / 소지품 / 상태 / 기록` 4개 탭으로 통합
- 소지품 상세 팝업 추가
- 선택지를 최초 화면에서 숨김
- 캐릭터가 관찰·길·단서 관련 힌트를 입력하면 행동 후보 공개
- 선택지 외 자유 입력으로 이동 경로 및 오브젝트 조사 판정
- 위험 중 방법을 질문하면 위험 대응 후보 공개
- 자유 행동을 판정하지 못한 경우에도 장면 반응을 자연어로 반환
- 같은 현장의 채팅·소지품 전달 기능 유지
- 이동 중 풍경 오버레이와 위험 경고 표시
- 장소 풍경용 흑백 하프톤 이미지 자산 추가

## v0.2.0

- 사건 로그 정렬을 시간 오름차순으로 변경
- 최신 사건 자동 스크롤
- 긴 로그 문장 줄바꿈과 가로 넘침 수정
- 복수 위험 장면을 완성된 자연어 한 문단으로 재구성
- 이동 중 UI와 1.8초 지연 처리
- 이동·위험·오염·도착 결과의 알림 통합
- 행동 성공·부분 성공·실패 서술 개선
- 별도 조사조의 현장 합류와 이탈 알림
- 현장 대화 기능
- 캐릭터 간 소지품 전달 기능
